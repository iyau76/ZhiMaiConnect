# 知脉 Connect 全功能人工 E2E 问题修复报告

- 修复日期：2026-08-29
- 分支：`refactor/relationship-core-20260828`
- 问题基线：`test-results/manual-e2e-20260829/剩余阶段续跑报告-2026-08-29.md`
- 产品判据：`doc/中期反思.md` v3

## 一、结论

本轮已经处理人工 E2E 暴露的四个发布阻断项，并完成真实 DeepSeek 双跑、全量测试、生产构建与浏览器回归。

最终结果：

| 验证层 | 结果 |
| --- | ---: |
| TypeScript、ESLint、Prettier | PASS |
| 单元与集成测试 | **58 文件，545 / 545 PASS** |
| 生产构建 | PASS |
| Playwright 全功能 E2E | **32 / 32 PASS** |
| 真实 API：未入库草稿纠错 | PASS |
| 真实 API：复杂关系连续双跑 | PASS |

本地复杂关系最终双跑均只调用模型一轮，稳定得到 27 人、26 条已确认事实关系、3 条待确认提案和 47 条可追溯投影。第 27 人是模型按原子关系契约创建的上下文占位实体“尤氏继母”。模型额外提出的“尤氏—尤二姐/尤三姐”和“贾政—赵姨娘”没有被冒充成原文事实，而是留在可见、可单独接受的 pending 账本中。

## 二、结构性改进

### 2.1 未入库草稿成为可寻址工作区

此前，未提交草稿只有一次模型输出中的 `plan:*` 临时引用。补充整理时，模型看到的是一份文本快照，无法稳定更新原草稿，因而会使用空 ID、旧 `plan:*` 或重新创建重复记录。

现在增加了正式的 intake workspace：

- 每条人物、事实、关系、事件、提醒和证据都有稳定 `draft:*` 引用；
- 工作区带 revision，补充轮次生成 update/delete/create，而不是重新生成一份互不相识的草稿；
- `plan:*` 在首轮编译后统一重绑定为 `draft:*`；
- 人工编辑字段、人工来源、身份决策和关系核对状态在后续补充中保留。

真实 API 结果：先生成“唐悦和周宁是同事”，不入库，再补充“刚才写错了，是前同事”，一轮完成原关系更新，没有重复人物、重复边或旧值残留。

证据：`test-results/live-adversarial/2026-08-29T08-59-46-595Z.json`。

### 2.2 关系从展示标签收敛到稳定语义身份

关系本体现在统一完成以下编译：

- 人物性别事实决定父母/子女角色，不再由模型措辞决定 `son` 或 `daughter`；
- “丈夫”“妻子”只作为展示语言，不再成为对称配偶关系的方向性身份；
- “妾”保留为关系级的 concubinage 区分；
- “继母的女儿”不会再被误判为 `step_parent_of`，而是识别为继同辈语义；
- 姑母、叔父、舅父、姨母、内侄等称谓统一进入关系本体；
- 本地投影仍只消费已确认事实断言，生成结果不回灌规则输入。

因此，同一事实即使模型分别输出“女儿”“母亲-女儿”，或“丈夫”“妻子-丈夫”，持久化语义身份也保持一致。

### 2.3 证据核验退出 Agent 重生成循环

一次真实失败揭示了明确的性能根因：模型首轮已在约 15 秒内返回完整的 54 项计划，但 basis 对原文做了轻微压缩。本地把“没有逐字复制”当作硬错误，要求模型重新输出整份计划；第二轮因长度截断又触发第三轮，最终耗尽 8k 输出 token。

现改为：

- ID、任务结构、日期格式和事务一致性仍是硬契约；
- basis 措辞、未对齐证据、模型推导和多跳称谓属于非底线核验；
- 本地优先把模型改写的 basis 对齐到同时包含关系两端与关系线索的原文片段；
- 无法对齐时保留草稿，并显示“AI 生成，请注意辨别”的具体原因；
- 不再为非底线核验重新生成整份计划，不吞掉结果，不抛内部错误给用户。

这落实了 `中期反思.md` 的 P8/P9：结构问题由结构承载，非底线核验只能成为可见提示，不能成为收费站。

关系依据现在按原子子句绑定，而不是只检查两个姓名是否曾在同一句中出现：

- 较长姓名会遮蔽内部的短姓名，“尤氏继母”不再同时算作“尤氏”；
- 关系词必须和两端处于同一子句；
- 只对明确的主语省略（如“王夫人是正妻，生了元春”）及“他们的儿子”一类指代做跨子句承接；
- 同一句中互不相关的前后分句不能再拼成幽灵边。

### 2.4 “问一问”的输出与工具语义

助理输出已采用类型化声明：

- `fact`：档案事实与引用；
- `gap`：具体人物、具体字段的缺失；
- `advice`：建议；
- `language`：普通语言回答；
- `uncertain`：资料不足或未核验内容。

`search_profiles` 现在明确返回 projection、returnedFields、omittedFields 和 detailTool。摘要工具没返回 likes，不再等价于“likes 为空”；字段级问题需要升级到详情读取。即使声明无法验证，内容也会落到“AI 生成内容（请注意辨别）”，不会静默消失。

真实 grounding/memory 场景已通过，证据见 `test-results/live-adversarial/2026-08-29T08-31-08-088Z.json`。

## 三、体验与数据闭环改进

- 月精度事件使用月份控件，年精度使用年份控件，不再强迫用户选择某一天。
- 事件增加原始时间表述；“去年夏天”等文本由本地模糊日期解析器生成范围并保留原文。
- 待确认内容改为软提醒；用户可直接入库，未接受的 AI 关系以 pending 保存且不参与亲属投影。
- “一键接受”只批量确认来源已对齐项目；证据未对齐关系展示具体原因并保留待确认，用户仍可逐条编辑、接受或删除。
- 补充整理保留人工修改和人工来源，不会被后一轮重新标成 AI。
- 同名人物和重复展示项使用稳定草稿/记录 ID 作为 React key。
- 英文界面的关键控件、提示、日历和设置文案已补齐。
- E2E 的事件更新用例现在等待事务完成再读取 IndexedDB，消除了并发回归中的读取竞态。

## 四、Cloudflare 性能改进

Worker 的 `/api/vision` 已从“逐 SSE chunk 解码、解析、重组、再编码”改为：

1. 完成请求校验与上游请求；
2. 收到上游响应头后，直接转发原始字节流；
3. 仅记录请求字节数、首响应头耗时、总墙钟时间、状态与流量等结构化指标。

这会显著减少长回答中的字符串分配、JSON 解析和高频拼接，也是针对历史 `exceededCpu` 的直接架构修正。客户端继续负责 SSE 解析。

本地构建、API 路由测试和真实长流均已通过。修复构建已部署为 Cloudflare Worker 版本 `8ba0fdda-28eb-46b2-bd39-49f824925afe`，同一复杂输入在线连续运行两次均成功返回，未出现 503，也没有重复生成整份计划。

本次 `wrangler tail` 的日志订阅连接本身发生 `ETIMEDOUT`，因此没有取得可比较的 `cpuTime` 样本。线上功能验收已经通过，但旧日志中的 `outcome=exceededCpu` 仍应在日志通道恢复后用 CPU 分布正式关闭，而不能只依据客户端成功响应推断。

## 五、真实 API 性能与稳定性

### 修复前的错误路径

| 项目 | 结果 |
| --- | ---: |
| 首轮输出 | 完整，约 9.3k 字符 |
| 本地证据校验 | 硬失败 |
| 第二轮 | 重发完整 50+ 项计划，输出被截断 |
| 第三轮 | 仅剩几十 token，格式不完整 |
| 总结果 | 两次均 `max_output_tokens` 失败，约 34 秒 |

### 修复后的正确路径

| 指标 | 双跑 1 | 双跑 2 |
| --- | ---: | ---: |
| 模型轮次 | 1 | 1 |
| 人物 | 27 | 27 |
| 已确认事实关系 | 26 | 26 |
| 待确认关系提案 | 3 | 3 |
| 派生关系 | 47 | 47 |
| 输出 token（估算） | 4,861 | 4,861 |
| 耗时 | 19.8 秒 | 19.8 秒 |
| 语义断言集合 | 一致 | 一致 |
| 关键关系与证据 | PASS | PASS |
| 投影可追溯性 | PASS | PASS |

证据：`test-results/live-adversarial/2026-08-29T09-35-52-005Z.json`。

相较失败路径，最重要的性能变化不是“平均快了几秒”，而是复杂请求不再因为非底线核验放大成 2–3 次完整生成；单用户的输出预算、上游调用次数和失败概率都回到了线性的一轮路径。

### Cloudflare 生产双跑

| 指标 | 线上 1 | 线上 2 |
| --- | ---: | ---: |
| HTTP / Agent 结果 | PASS | PASS |
| 模型轮次 | 1 | 1 |
| 人物 | 27 | 27 |
| 模型关系提案 | 27 | 28 |
| 已确认事实关系 | 26 | 26 |
| 待确认关系提案 | 1 | 2 |
| 派生关系 | 47 | 47 |
| 输出 token（估算） | 4,194 | 4,795 |
| 耗时 | 17.5 秒 | 22.1 秒 |
| 已确认语义集合 | 一致 | 一致 |

模型第二次多提出了一条“尤氏—尤氏继母”提案，但原子子句审计把它保持为 pending；事实图和派生图没有随模型随机性漂移。这正是“提案层允许不确定、事实层保持稳定”的验收目标。

证据：`test-results/live-adversarial/2026-08-29T09-45-04-633Z.json`。

## 六、验证命令与结果

```powershell
npm run check
npm run build
npm run e2e
$env:ZHIMAI_LIVE_SCENARIOS='complex'; node scripts/live-adversarial-test.mjs
$env:ZHIMAI_LIVE_SCENARIOS='complex,draft-correction'; node scripts/live-adversarial-test.mjs
```

结果：

- `npm run check`：PASS，58 个测试文件、545 项测试；
- `npm run build`：PASS；
- `npm run e2e`：PASS，32 项；
- 真实 complex 双跑：PASS；
- 真实 draft-correction：PASS。

生产构建仍有一个非阻断警告：主 routes chunk 超过 500 kB。它不会造成当前功能错误，但后续应把 PDF/DOCX 导入导出、关系图和非首屏面板改成按需加载，降低首次下载与解析成本。

## 七、上线状态与剩余观察项

已完成：

1. 部署当前 Worker 构建；
2. 对线上 `/api/vision` 连续执行两次复杂录入；
3. 验证两个请求均一轮完成、无 503，事实和派生语义稳定；
4. 确认线上版本号为 `8ba0fdda-28eb-46b2-bd39-49f824925afe`。

剩余观察项：

1. 日志订阅网络恢复后，用 `wrangler tail` 或 Workers Logs 记录 status、outcome、cpuTime、wallTime、requestBytes、streamBytes；
2. 至少保留一组高输出请求 CPU 样本；若仍出现 `exceededCpu`，再根据新日志判断套餐 CPU 上限；
3. 人工重新执行 Chrome 的 S05 与 I02，覆盖用户本机网络与浏览器扩展环境。

当前结论是“代码层、本地真实 API 和线上功能验收通过；本次复杂双跑未复现 503；Cloudflare CPU 分布仍待日志取证”，不把两次成功请求夸大成对所有网络条件的永久保证。
