# 知脉 Connect 项目记忆

只放核心哲学、硬约束、稳定不变量；其余从代码、测试与 doc/ 现行文档推导。
判据：`doc/product/中期反思.md`（P1–P9 全文）｜叙事与边界：`doc/background/总复盘报告.md`｜归档契约：`doc/architecture/机器归档格式-v2.md`。
历史报告只能追溯，不能代替代码、测试和现行契约。

## 一、产品与叙事

- 一句话：过去只有少数人请得起秘书。现在，每个人都可以拥有自己的关系秘书——替你记住人、记住事，帮你想清楚下一步该找谁。
- 秘书三样本事：记得住、理得清、做得有分寸，绝不擅自替主人发消息、做决定。主闭环：低摩擦录入 → 人工确认入库 → 有依据地行动。
- 不做：不读微信/QQ 等平台，不自动外发，推断不当事实写入，不宣称“绝对安全/完全离线”，初评不做多端与云同步。
- 冻结演示：校园记忆展——唐悦推荐学妹林柚做海报；两个王晨靠身份消歧；“找谁拍照”前三为唐悦、叶青、秦月（`7c688c7` 重建后，材料以此为准）。

## 二、核心哲学

- 理解归模型，事实归账本，批准归人。程序是会计，AI 是参谋，人是老板。批准是一张可撤销的收据，不是收费站。
- 壁垒是验证成本：LLM 降低了理解成本，没有降低验证成本——让依据、溯源、撤销、一键批准最便宜。
- 人的档案是关系束：关系、圈层、事件、提醒与“人”平级。差序以“我”为原点，六因素由本地排序，AI 只解释不改顺序；分数只回答“这次该找谁”，不给社会价值排行。
- P8 结构优先于补丁：先问缺了哪类数据表达或唯一通道，再决定加规则加工具；每个局部机制必须说明并入哪个现有结构。正例：U1 加 gap 专用工具是补丁，类型化声明 fact/gap/advice/uncertain/language 才是正解。
- P9 用户优先于非底线核验：底线（急症、合规、不外发、不泄密）永不放松；非底线核验失败只能降级为可见“未核验”——绝不隐藏答案、空回答、多轮重试折磨用户；轻微措辞差异只回炉出错条目。
- P1–P7 全文见中期反思 3.6；一句话版：谁生产事实谁承担审计、确定性只投影、语义归模型执行归本地、正则配正反例、写入单通道、用户不承担本地规则的错误、信任可调账本不可调。

## 三、硬约束

- 不夸大：数据默认只在浏览器 IndexedDB；云端只收当前任务所需最小内容；没有登录、云库、多端同步就写没有。我们不说掌握了客观真理，也不经营用户关系数据。
- 密钥只进 .env.local 或 Cloudflare Secret；日志、文档、截图、夹具、提交禁真实联系人、API Key、Cookie 与原始敏感材料；演示与测试只用合成数据。
- 严禁防御性编程、防御性写作；删宣传腔、AI 套话、模糊归因、空泛结论；保持用户已有改动，不顺手格式化或改写无关文件。
- 停止门槛：用户只夸图不维护数据 → 停加连接器，回头验证价值；核心价值若必须依赖自动读微信 → 停该路线。
- 顶部 Lovable 约束适用：不重写已发布历史，保持分支可工作。

## 四、架构不变量

- 每类事实单一权威源；一等记录靠稳定 ID 连接；投影可重建、随源变化失效、不写回事实表；备份按 archive@2，展示导出不冒充备份，导入丢弃派生重建。
- 圈层以 collections/memberships 为唯一真相；删除人物显式处理其事件、提醒、事务与证据引用；同名不是合并键，引用无法唯一确定时拦截提交。
- 统一 Agent 机制：共享工具注册（public_read/private_read/network/proposal/write）、预算、权限、日志、恢复与事务提交；写入只走事务协调器；模型输出统一为类型化声明。
- 渐进披露：先索引后取档；联系方式只给 hasContact；not_loaded 必须带 detailTool；没有工具结果不得声称已读档案；断点续跑复用已有结果，不从头检索。
- 提示词、schema、解析器、工具注册、执行器同步修改并用契约测试覆盖；动 schema、归档、稳定 ID 前先设计迁移、回滚与恢复。

## 五、写作与语言

- 用“我们”；先写具体场景与事实，再给抽象概念；长短句交替；术语首次出现给人话解释。
- 禁“不是……而是……”式否定排比（确有对照才用）；删过量粗体与装饰标题。
- 叙事与旁白禁词：账本、审计、契约（技术文档可用，克制）；Agent Loop 只作对象或定语，不作叙事主语。
- 明确“已实现/已知限制/后续计划”；带日期的测试数字、旧分支名、临时故障不进长期文档；脚本单源冻结。

## 六、验证与发布

- 合并或部署前运行 `npm run check`、`npm run build`、`npm run e2e`；check 必须全绿。
- Cloudflare 只部署 main 构建产物；部署后核对 Worker 版本与静态资源指纹。
- 根 README 面向用户、评委、开发者及其智能体：可运行版本、最短体验路径、架构边界、验证命令、隐私说明。
- 现行规范放 doc/product|architecture|quality|research；阶段报告进 doc/history/<年月>/；身份与参赛材料进已忽略的 doc/background/；videos/ 不提交。
