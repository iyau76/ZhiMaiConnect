<!-- LOVABLE:BEGIN -->
> [!IMPORTANT]
> This project is connected to [Lovable](https://lovable.dev). Avoid rewriting
> published git history — force pushing, or rebasing/amending/squashing commits
> that are already pushed — as it rewrites history on Lovable's side and the
> user will likely lose their project history.
>
> Commits you push to the connected branch sync back to Lovable and show up in
> the editor, so keep the branch in a working state.
<!-- LOVABLE:END -->

# 知脉 Connect 项目规范

本文件适用于整个仓库。现行产品哲学见
[`doc/product/中期反思.md`](doc/product/中期反思.md)，数据归档契约见
[`doc/architecture/机器归档格式-v2.md`](doc/architecture/机器归档格式-v2.md)。历史报告只能用于追溯，不能代替代码、测试和现行契约。

## 产品目标

- 知脉 Connect 是面向个人的关系记忆与行动助手。它帮助用户记住人、关系、事件和待办，并在需要联系、回忆或决策时提供可追溯的帮助。
- 核心流程必须形成闭环：自然语言或材料录入 → AI 理解与提案 → 用户批准 → 本地事务写入 → 可查询、可修改、可撤销、可备份。
- 产品面向真实生活中的含混表达和持续变化。设计应降低整理与核对成本，避免把结构化负担转嫁给用户。
- 当前个人版默认把档案保存在浏览器 IndexedDB。云端模型只接收完成当前任务所需的内容；没有实现的登录、云数据库和多设备同步不得写成现有能力。

## 三方边界

长期遵循：**理解归模型，事实归账本，批准归人。**

- 模型负责理解自由文本、消解指代、选择工具、形成解释和结构化提案。
- 本地账本负责稳定 ID、类型、引用完整性、事务、版本、撤销、投影与确定性校验。
- 用户决定持久写入和敏感操作。批准界面应像一张清楚的收据，支持汇总批准、逐项检查和撤销。
- AI 生成的推断、建议和未核验内容要有清晰标识与依据。非底线不确定性采用软提醒，不能通过隐藏答案、空回答或反复核验阻断任务。
- 密钥泄露、未经授权的外发、不可恢复删除、急症安全等底线风险保留明确门禁。

## 数据与关系模型

- 每类事实只有一个权威来源。禁止用“可选字段 + 各消费方自行补默认值”制造多套真相。
- 人物、关系断言、证据、事件、提醒、任务、项目、集合和成员关系都是一等记录，使用稳定 ID 连接。不得靠显示姓名或自由文本维持引用。
- `relationAssertions` 保存用户确认或材料明确支持的关系事实；亲属链、路径和排序等派生结果属于 projection。投影必须能列出支持它的 assertion，并在源事实变化后重新计算。
- 派生关系不写回事实表，不与模型推导形成双轨真相。删除或修改源事实后，依赖投影必须自动失效。
- 圈层以 `collections` / `collectionMemberships` 为权威数据；拓扑社区是计算视图。展示偏好、引荐策略和关系事实分别保存。
- 事件应能通过 ID 连接人物、来源和相关任务。删除人物时要显式处理事件、提醒、事务和证据引用，不能留下幽灵记录。
- 机器备份使用 `zhimai-connect/archive@2`。展示导出不能冒充可恢复备份；导入时丢弃派生快照并按当前投影器重建。
- 数据模型升级必须提供明确迁移和引用校验。兼容逻辑收口在输入边界，不能扩散到每个消费方。

## Agent 与工具架构

- 所有 Agent 共用统一的工具注册、预算、权限、日志、错误恢复和事务提交机制。禁止新增一套私有工具映射、解析循环或提案分发器。
- 工具表达稳定的领域能力，例如检索人物、读取档案、查询关系、提交变更。禁止为了某条测试问句或某个专名增加专用工具、关键词分支或提示词答案。
- 语义理解交给模型；本地代码只做能够证明正确的结构校验、闭包、排序和事务操作。正则或词表参与语义判断时，必须有正反例语料并说明适用边界。
- Agent 使用渐进披露：先索引，再按需读取档案、关系和事件。不能盲目发送全库，也不能在没有实际工具结果时声称已经分析档案。
- 多轮会话应复用已经取得的工具结果。5xx、超时和中断恢复要保留检查点与工具历史，继续执行时不得从头重复检索。
- 模型输出统一进入类型化声明与提案通道。`fact`、`gap`、`advice`、`language`、`uncertain` 各有明确渲染语义；单条引用失败只影响该引用的核验状态，不能吞掉整段答案。
- 写入统一经过事务协调器。提示词、schema、解析器、工具注册和执行器变更必须同步，并用契约测试覆盖。

## 工程取舍

- **严禁防御性编程、防御性写作。**
- 发现问题先定位缺失的数据表达、领域边界或唯一执行路径。结构错误应重构；禁止把连续增加按钮、校验层、重试分支和局部 if-else 当作长期方案。
- 共享规则下沉到领域模块。权限、默认值、关系 label、错误分类和重试策略不得在调用点复制。
- 测试覆盖能力边界、状态变化和失败恢复，避免面向测试集编程。外部服务使用可控替身完成自动化测试，真实 API 验收单独执行且不保存密钥和私人正文。
- 修改必须兼顾既有本地数据。涉及 IndexedDB schema、归档格式或稳定 ID 时，先设计迁移、回滚与恢复验证。
- 保持工作树中用户已有改动；不要顺手格式化或改写无关文件。

## 交互准则

- 优先减少填写、点击、往返和人工逐条修补。批量操作要提供清楚的差异摘要、局部展开和整体撤销。
- 页面先呈现用户当前要完成的任务，再逐步披露高级设置、审计轨迹和模型预算。
- 关系图的布局、筛选和聚焦服务于找人、理解群体和解释路径。计算视图不得伪装成用户事实。
- AI 状态应展示模型真实返回的流式内容、工具调用和可理解的进度；禁止伪造思维过程或用循环动画掩盖停滞。
- 桌面端要控制信息密度、画布高度和滚轮行为；移动端与键盘操作保持可达。表单控件必须有名称、标签和可见焦点。
- 错误信息说明发生了什么、保留了什么、下一步能做什么。内部 schema、堆栈和供应商响应正文不直接抛给普通用户。

## 文档与写作

- 根目录 `README.md` 面向首次访问的用户、评委、开发者及其智能体，必须准确描述当前可运行版本，并提供最短体验路径、架构边界、验证命令和隐私说明。
- 现行规范放入 `doc/product/`、`doc/architecture/`、`doc/quality/` 或 `doc/research/`；阶段报告进入 `doc/history/<年月>/`；身份信息、参赛材料、原始聊天、本机审计进入已忽略的 `doc/background/`。`videos/` 不提交。
- 明确区分“已经实现”“已知限制”“后续计划”。带日期的测试数字、旧分支名、临时部署故障和阶段性口号不进入长期说明。
- 先写事实和结果，再写原因。使用具体名词、可验证路径和可复制命令；术语首次出现时给出简短解释。
- 删除宣传腔、模糊归因、AI 套话、过程痕迹、空泛结论、过量粗体和装饰性标题。避免“不是……而是……”“是……，不是……”等否定式排比，确有逻辑对照时才使用。
- 不夸大隐私、安全、智能程度或部署成熟度。隐私说明必须同时交代浏览器本地存储、模型请求边界、备份责任和当前尚未提供的同步能力。

## 验证与发布

- 常规改动至少运行与风险相称的定向测试。合并或部署前运行：

  ```powershell
  npm run check
  npm run build
  npm run e2e
  ```

- `npm run check` 必须保持类型检查、ESLint、Prettier 和单元测试全绿。构建警告要记录并判断是否影响发布，不能只看退出码。
- Cloudflare 只部署当前 `main` 构建出的 `.output/server/wrangler.json`。部署后核对 Worker 版本、公开页面和静态资源指纹。
- 密钥只进入 `.env.local` 或 Cloudflare Secret。日志、截图、测试夹具、提交记录和文档中禁止出现真实联系人信息、API Key、Cookie 或原始敏感材料。
- 保持 Git 历史可追溯。遵守上方 Lovable 约束，禁止 force push、已发布提交的 rebase/amend/squash。
