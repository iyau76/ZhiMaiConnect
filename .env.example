# Optional comma-separated exact hostnames allowed by the cloud AI server proxy.
# OpenAI, DeepSeek and Google Gemini official hosts are already allowed.
# Wildcards, IP literals, local names, ports, paths, and schemes are not accepted here.
AI_PROXY_ALLOWED_HOSTS=

# Optional secret salt for pseudonymous Cloudflare rate-limit client buckets.
# In production set it with `wrangler secret put`, never commit the real value.
ZHIMAI_RATE_LIMIT_SALT=
